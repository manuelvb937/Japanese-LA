# Backend host settings
HOST=0.0.0.0
PORT=8000

# Optional override for local development.
# Prefer Application Default Credentials:
#   gcloud auth application-default login
#   gcloud auth application-default set-quota-project PROJECT_ID
# GOOGLE_APPLICATION_CREDENTIALS=/absolute/path/to/service-account.json

# Optional: set this to test the backend/frontend loop without Google Vision.
# MOCK_OCR_TEXT=手打ちうどんは、だしの香りをお楽しみください。
